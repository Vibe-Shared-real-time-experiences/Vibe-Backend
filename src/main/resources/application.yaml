server:
  port: 8080
  tomcat:
    threads:
      max: 100
spring:
  config:
    import: "optional:file:app.env[.properties]"
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/vibe_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:root}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 10
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        pool:
          # Default is 8 connections
          max-active: 40
          max-idle: 20
          min-idle: 5
          max-wait: 500ms
  cache:
    type: redis

# MinIO / S3 Configuration
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: vibe-storage
  region: us-east-1

security:
  jwt:
    secret-key: "91f6b3d88bdd0c39fa408b0ee613cdd4e83c774c9440e96c3107e1dc8604a4c8"
    issuer: "VibeApp"
    expiration-ms: 3600
    refresh-expiration-ms: 604800

node-id: "1"

#logging:
#  level:
#    ROOT: ERROR